import subprocess
index()

def index():
    subprocess.call("sudo hostnamectl set-hostname 'AzaanSpeaker'", shell=True)

index()

import subprocess

def index():
    subprocess.call("passswd", shell=True)

index()
import subprocess

def index():
    subprocess.call("sudo apt-get update", shell=True)

index()
import subprocess

def index():
    subprocess.call ("sudo apt full-upgrade -y", shell=True)

#index()

import subprocess

def index():
    subprocess.call("sudo apt-get install omxplayer -y", shell=True)

index()

import subprocess

def index():
    subprocess.call("sudo apt-get install git -y", shell=True)

index()

import subprocess

def index():
    subprocess.call("sudo apt-get install raspi-gpio -y", shell=True)

index()

import subprocess

def index():
    subprocess.call("sudo service cron start", shell=True)

index()

import subprocess

def index():
    subprocess.call("sudo timedatectl set-timezone America/New_York", shell=True)

index()

import subprocess

def index():
    subprocess.call("sudo timedatectl set-ntp true", shell=True)

index()

import subprocess

def index():
    subprocess.call("date", shell=True)

index()


import subprocess

def index():
    subprocess.call("sudo systemctl enable ssh", shell=True)

index()


#import subprocess

#def index():
   # subprocess.call("sudo apt install filezilla", shell=True)

#index()


import subprocess

def index():
    subprocess.call("sudo systemctl start ssh", shell=True)

index()

import subprocess

def index():
    subprocess.call("sudo apt install -y fonts-freefont-ttf", shell=True)

index()

import subprocess

def index():
    subprocess.call("sudo apt-get clean", shell=True)

index()

import subprocess

def index():
    subprocess.call("sudo apt-get install xrdp -y", shell=True)

index()
import subprocess

def index():
    subprocess.call("sudo raspi-config --expand-rootfs ", shell=True)

index()

import subprocess

def index():
    subprocess.call("git clone git@github.com:pakmian/adhan.git", shell=True)

index()


import subprocess

def index():
    subprocess.call("python /home/pi/adhan/updateAzaanTimers.py", shell=True)

index()

import subprocess

def index():
    subprocess.call("df -h", shell=True)
index()